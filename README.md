# MICROLEARN-C
Microlearn-c is a lightweight Python tool that loads scikit-learn models and automatically generates standalone C inference code (no external dependencies) for deployment on microcontrollers and other constrained devices.
